global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "alert.rules.yml"

scrape_configs:
  - job_name: "taskmanager-api"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["app:3000"]
